# 📦 Importing Classes from Other Packages

This Java program demonstrates how to create a class inside a package and import that class into another Java program.

The `Calculator` class is created inside the `calculator` package and imported into `PackageDemo` using the `import` statement.

## 📂 Program Structure

```text
29-Importing-Packages/
│
├── calculator/
│   └── Calculator.java
│
└── PackageDemo.java
